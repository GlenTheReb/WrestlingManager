{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The local application window. No filesystem, shell or network plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
